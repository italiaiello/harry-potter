import React from 'react'

const HouseDetails = ({ dispatch, data }) => {

    console.log(data)

    return (
        <div>
            <h1>House Details</h1>
        </div>
    )
}

export default HouseDetails
